<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoggingViewsAndActivity">
        <activity android:name=".LessonListActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".LessonRatingActivity"
            android:parentActivityName=".LessonListActivity"/>
        <activity android:name=".RatingActivity" />
        <activity android:name=".ViewsActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>